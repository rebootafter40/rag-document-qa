# Core dependencies for the RAG Document Q&A app.
# Only direct imports are listed; pip resolves transitive deps (torch, numpy,
# tokenizers, etc.) automatically. Dev tools (black, ruff, pytest) are
# intentionally excluded — they're not needed at runtime.

anthropic==0.80.0
chromadb==1.5.0
pydantic-settings==2.14.1
PyMuPDF==1.27.1
sentence-transformers==5.3.0
streamlit==1.54.0
