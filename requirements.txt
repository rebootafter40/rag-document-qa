# Core dependencies for the RAG Document Q&A app.
# Dev tools (black, ruff, pytest) intentionally excluded — not needed at runtime.
# CPU-only torch: Streamlit Cloud has no GPU, so the CUDA build wastes memory.
--extra-index-url https://download.pytorch.org/whl/cpu
anthropic==0.80.0
chromadb==1.5.0
pydantic-settings==2.14.1
PyMuPDF==1.27.1
sentence-transformers==5.3.0
streamlit==1.54.0
